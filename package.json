{
  "name": "m-fs",
  "version": "1.0.0",
  "description": "Promise version of common Node.js fs methods",
  "main": "dist/lib/main.js",
  "types": "dist/lib/main.d.ts",
  "author": "Mgen (https://www.mgenware.com)",
  "homepage": "https://github.com/mgenware/m-fs",
  "bugs": "https://github.com/mgenware/m-fs/issues",
  "keywords": [
    "fs",
    "promise"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/mgenware/m-fs.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^22.0.1",
    "@types/node": "*",
    "copyfiles": "*",
    "jest": "^22.0.4",
    "tslint": "*",
    "typescript": "*"
  },
  "scripts": {
    "test": "yarn run clean && yarn run compile && yarn run lint && yarn run copy && yarn dev_test",
    "dev_test": "jest",
    "build": "yarn test",
    "compile": "tsc",
    "dev": "yarn run clean && tsc -w",
    "prepublishOnly": "yarn run test",
    "lint": "tslint --project .",
    "copy": "copyfiles \"__tests__/data/**/*.*\" \"dist\"",
    "clean:dist": "rm -rf dist",
    "clean": "npm run clean:dist"
  },
  "engines": {
    "node": ">=6.0.0"
  },
  "dependencies": {
    "bluebird": "^3.5.1",
    "mkdirp-promise": "^5.0.1",
    "node-filter-async": "^0.0.3"
  }
}
